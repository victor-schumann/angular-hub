'use strict'

const constants = require('karma/lib/constants')
const Server = require('karma/lib/server')
const runner = require('karma/lib/runner')
const stopper = require('karma/lib/stopper')
const launcher = require('karma/lib/launcher')
const cfg = require('karma/lib/config')

module.exports = {
  constants: constants,
  VERSION: constants.VERSION,
  Server: Server,
  runner: runner,
  stopper: stopper,
  launcher: launcher,
  config: { parseConfig: cfg.parseConfig } // lets start with only opening up the `parseConfig` api
}
